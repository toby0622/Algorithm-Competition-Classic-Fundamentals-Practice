#include <stdio.h>
#include <math.h>

int main(void)
{
    printf("%.8f\n", 1 + 2 * sqrt(3) / (5 - 0.1));

    return 0;
}

// integer - float = float (integer change to float, and become float - float = float)

// function sqrt is for calculating the square root of the specific number